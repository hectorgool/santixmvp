<%= simple_form_for [tour, reservation] do |f| %>

  <%= f.input :customers %>

  <%= f.input :date %>

  <%= f.input :schedule %>

  <%= f.button :submit, class: 'btn btn-info btn-lg btn-block' %>

<% end %>
