<%= simple_form_for( @reservations ) do |f| %>

  <%= f.input :customers %>

  <%= f.input :date %>

  <%= f.input :time %>

  <%= f.button :submit, class: 'btn btn-info btn-lg btn-block' %>

<% end %>
